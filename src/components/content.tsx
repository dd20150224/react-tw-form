// import Counter from './counter';
import FormLayout from './form/formLayout';

const Content = ({initialCount=0}) => {
  return (
    // <Counter initialCount={0} />
    <FormLayout></FormLayout>
  )
}

export default Content;
