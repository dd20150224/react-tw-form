export interface IFieldConfig {
  id: string;
  label: string;
  type: string;
}

export interface IRecord {
  displayName: string;
  avatarUrl: string;
  alias: string;
  age: number;
  price: number;
}